# 네이버 통합검색 블로그 순위 확인

## 실행

이 폴더의 **start.cmd**를 더블클릭하세요. 실행 창을 열어둔 채 브라우저에서 http://127.0.0.1:3210 을 사용하세요. 처음에 연결되지 않으면 잠시 후 새로고침하세요. 종료하려면 실행 창에서 Ctrl+C를 누르세요.

현재 PC의 Codex에 포함된 Node.js·Playwright와 설치된 Microsoft Edge로 실행됩니다. 다른 PC에서는 Node.js 22 이상 설치 후 이 폴더에서 `npm install`을 실행하세요. Edge가 없다면 `npx playwright install chromium`도 실행하세요.

## 테스트

검색 키워드에 `창원 불면증`, 블로그명에 `창원휴한의원`, 범위에 `20위까지`를 선택해 순위 확인을 누르세요. 이 이름과 `휴한의원 창원`은 서로 다른 문자열입니다. 반드시 검색 결과에 표시된 이름을 입력하세요.

**test.cmd**를 더블클릭하면 20개 결과의 제목·종류·블로그명·주소를 출력합니다. `debug/page-1.png`는 실제 검색 화면이고 `debug/report.json`은 수집 결과입니다. 화면과 목록을 함께 비교할 수 있습니다.

명령어로 100개를 검사하려면 `node scripts/probe.mjs "창원 불면증" "휴한의원 창원" 100`을 실행하세요. 기본 검증은 `node --test`입니다.

## 순위 기준

- 로그인하지 않은 PC 통합검색, 1440px 너비 기준입니다.
- 광고·플레이스·쇼핑 서비스·연관 검색어 영역을 제외합니다.
- 웹사이트·블로그·지식iN·카페 등 **독립된 일반 결과 카드의 대표 제목**을 위에서 아래로, 같은 높이면 왼쪽에서 오른쪽 순서로 셉니다. 카드 안의 댓글·관련 글 바로가기는 별도 결과로 세지 않습니다.
- 다음 통합검색 페이지가 필요하면 화면에 있는 페이지 링크를 따라가고 표시된 카드 수를 누적합니다. 블로그/VIEW 탭으로 전환하지 않습니다. 여러 페이지에 같은 글이 다시 나타나면 각각의 노출로 셉니다.
- 앞뒤 공백·연속 공백만 정리하고 이름 전체를 비교합니다. 비슷한 이름이나 글 본문의 이름은 일치로 판단하지 않습니다.
- 요청 범위까지 읽지 못하면 ‘없음’이라고 단정하지 않고 오류를 표시합니다. 여러 노출은 내부 `matches` 배열에 보존합니다.

네이버가 공식으로 제공하는 순위가 아니라 위 기준으로 계산한 관측 순위입니다. 시점·지역·로그인·기기에 따라 달라질 수 있습니다. 현재 검증한 검색 화면 구조를 지원하는 초기 버전으로, 모든 키워드의 특수 결과 영역을 보장하지 않습니다. 인식하지 못하는 구조가 발견되면 오류로 중단합니다.

## 문제가 생기면

입력한 키워드·블로그명·검색 범위와 화면의 한국어 오류 문구를 보내주세요. 실행되지 않으면 검은 실행 창의 메시지를 보내주세요. 순서가 다르면 같은 시점의 네이버 통합검색 화면과 `debug` 폴더의 결과를 함께 보내주세요.

## 파일 역할

`public/`: 웹 화면 / `src/collector.mjs`: 통합검색 접속·페이지 이동 / `src/exclusions.mjs`: 제외 기준 / `src/extract.mjs`: 제목·출처·화면 위치 추출 / `src/ranking.mjs`: 정렬·이름 비교·순위 / `src/server.mjs`: 화면과 수집 연결.

회원가입·데이터베이스·결제·기록 저장·자동 예약은 없습니다. 현재 PC에서만 접속하는 로컬 웹서비스이며 외부 배포는 하지 않았습니다.

## 여러 키워드 · 여러 블로그 비교

검색 키워드와 찾을 블로그명을 각각 **한 줄에 하나씩** 입력하세요. 각 입력은 최대 20개이며 빈 줄과 같은 항목의 중복은 자동으로 정리합니다. 예를 들어 키워드 5개, 블로그명 2개를 입력하면 5행 × 2열로 각 조합의 순위를 보여줍니다. 공백이나 쉼표는 항목 구분자가 아닙니다.

검색은 키워드별로 한 번씩 순서대로 진행하며 완료된 행부터 결과를 표시합니다. ‘발견된 글’을 펼치면 해당 글의 제목과 링크가 나옵니다. ‘20위 내 없음’은 선택한 범위에서 이름이 일치하는 블로그를 찾지 못했다는 뜻이고, ‘조회 실패’는 검색을 완료하지 못했다는 뜻입니다. 일부 키워드가 실패해도 다음 키워드는 계속 확인합니다. 조회 중에는 이 화면을 열어두세요. 새로고침하면 표가 초기화됩니다.
